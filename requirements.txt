# ============================================================
# LG_Project 의존성
# 배포: 웹(Streamlit) → Docker / LLM·임베딩: 사내·로컬 모델
# 설치: pip install -r requirements.txt
# ============================================================

# --- UI / 웹 ---
streamlit>=1.40,<2.0

# --- 데이터 처리 ---
pandas>=2.2
numpy>=1.26
openpyxl>=3.1            # XLSX 읽기/쓰기 (USR-001, ANA-001)

# --- 시각화 ---
plotly>=5.24            # 시계열 그래프, 이상 구간 표시 (USR-005, USR-006)

# --- DB (ORM, SQLite 기본 / PostgreSQL 전환 가능) ---
SQLAlchemy>=2.0
# psycopg2-binary>=2.9   # PostgreSQL 전환 시 주석 해제

# --- RAG (로컬 벡터DB + 로컬 임베딩) ---
chromadb>=0.5           # 로컬 벡터 저장/검색 (RAG-001, RAG-002)
sentence-transformers>=3.0   # 로컬 임베딩 (torch 자동 설치, 데이터 외부 유출 없음)

# --- LLM (사내/로컬 모델 호출) ---
httpx>=0.27             # 로컬 LLM 서버(Ollama 등) HTTP 호출 (LLM-001~003)

# --- 리포트 생성 ---
jinja2>=3.1             # HTML 템플릿 (RPT-002)
weasyprint>=62          # HTML → PDF 변환 (RPT-002)

# --- 설정/환경 ---
pydantic-settings>=2.4
python-dotenv>=1.0
