# ============================================================
# 환경 변수 예시. 복사해서 .env 로 만들고 값을 채우세요.
#   cp .env.example .env   (Windows: copy .env.example .env)
# .env 는 git에 커밋하지 않습니다 (.gitignore 등록됨).
# ============================================================

# --- DB ---
# SQLite(기본). PostgreSQL 전환 시:
#   DB_URL=postgresql+psycopg2://user:pass@db:5432/lgproject
DB_URL=sqlite:///data/app.db

# --- 로컬 LLM 서버 (Ollama 등) ---
OLLAMA_URL=http://localhost:11434
LLM_MODEL=llama3.1:8b

# --- 로컬 임베딩 모델 (sentence-transformers) ---
# 한국어 데이터에 적합한 모델 예시
EMBEDDING_MODEL=jhgan/ko-sroberta-multitask

# --- 벡터 DB(Chroma) 저장 경로 ---
CHROMA_DIR=data/chroma

# --- 데이터 경로 ---
UPLOAD_DIR=data/uploads
REPORT_DIR=data/reports

# --- 접근 제어 (ADM-001: 엔지니어 화면 보호용 간단 키) ---
ENGINEER_ACCESS_CODE=change-me
